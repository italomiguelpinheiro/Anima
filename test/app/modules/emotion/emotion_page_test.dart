import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:anima/app/modules/emotion/emotion_page.dart';

void main() {
  testWidgets('EmotionPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EmotionPage(title: 'Emotion')));
    //  final titleFinder = find.text('Emotion');
    //  expect(titleFinder, findsOneWidget);
  });
}
