import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:anima/app/modules/report/report_controller.dart';
import 'package:anima/app/modules/report/report_module.dart';

void main() {
  initModule(ReportModule());
  // ReportController report;
  //
  setUp(() {
    //     report = ReportModule.to.get<ReportController>();
  });

  group('ReportController Test', () {
    //   test("First Test", () {
    //     expect(report, isInstanceOf<ReportController>());
    //   });

    //   test("Set Value", () {
    //     expect(report.value, equals(0));
    //     report.increment();
    //     expect(report.value, equals(1));
    //   });
  });
}
