import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:anima/app/modules/reminder/reminder_page.dart';

void main() {
  testWidgets('ReminderPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ReminderPage(title: 'Reminder')));
    //  final titleFinder = find.text('Reminder');
    //  expect(titleFinder, findsOneWidget);
  });
}
