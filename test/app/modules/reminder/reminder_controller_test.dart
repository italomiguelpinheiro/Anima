import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:anima/app/modules/reminder/reminder_controller.dart';
import 'package:anima/app/modules/reminder/reminder_module.dart';

void main() {
  initModule(ReminderModule());
  // ReminderController reminder;
  //
  setUp(() {
    //     reminder = ReminderModule.to.get<ReminderController>();
  });

  group('ReminderController Test', () {
    //   test("First Test", () {
    //     expect(reminder, isInstanceOf<ReminderController>());
    //   });

    //   test("Set Value", () {
    //     expect(reminder.value, equals(0));
    //     reminder.increment();
    //     expect(reminder.value, equals(1));
    //   });
  });
}
