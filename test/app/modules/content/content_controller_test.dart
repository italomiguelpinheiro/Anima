import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:anima/app/modules/content/content_controller.dart';
import 'package:anima/app/modules/content/content_module.dart';

void main() {
  initModule(ContentModule());
  // ContentController content;
  //
  setUp(() {
    //     content = ContentModule.to.get<ContentController>();
  });

  group('ContentController Test', () {
    //   test("First Test", () {
    //     expect(content, isInstanceOf<ContentController>());
    //   });

    //   test("Set Value", () {
    //     expect(content.value, equals(0));
    //     content.increment();
    //     expect(content.value, equals(1));
    //   });
  });
}
