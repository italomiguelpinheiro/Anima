import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:anima/app/modules/content/content_page.dart';

void main() {
  testWidgets('ContentPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContentPage(title: 'Content')));
    //  final titleFinder = find.text('Content');
    //  expect(titleFinder, findsOneWidget);
  });
}
